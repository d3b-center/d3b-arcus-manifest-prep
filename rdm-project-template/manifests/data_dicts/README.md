## `manifests/data_dicts/`

This directory contains data dictionaries for each of the metadata files contained with-in the parent [`manifests/`](../manifests) directory.

## Admin Notes

This sub-directory is required.
